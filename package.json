{
  "name": "@slowdini/superslow-opencode",
  "private": true,
  "version": "2.0.0",
  "description": "Superslow — a fork of obra/superpowers, rebranded as its own product",
  "type": "module",
  "main": "./opencode/plugins/superpowers.js",
  "files": [
    "opencode/",
    "skills/",
    "bootstrap.md"
  ],
  "scripts": {
    "test": "bun test --pass-with-no-tests",
    "test:codex": "cd tests/codex && bash test-sync-to-codex-plugin.sh",
    "test:opencode": "cd tests/opencode && bash run-tests.sh",
    "evals": "bun run tests/skill-evals/run.ts",
    "evals:snapshot": "bun run tests/skill-evals/run.ts snapshot",
    "evals:aggregate": "bun run tests/skill-evals/aggregate.ts",
    "version": "node scripts/bump-version.js",
    "check": "biome check --write . && markdownlint-cli2 --fix '**/*.md' '!**/node_modules/**' '!**/.worktrees/**'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4",
    "markdownlint-cli2": "^0.22.1"
  }
}
